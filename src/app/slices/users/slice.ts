import { createAsyncThunk } from '@reduxjs/toolkit';

// import { userApi } from './services';

// export const registerUser = createAsyncThunk('users/register', async (registerInfo, { rejectWithValue }) => {
//   try {
//     const res = await userApi.register(registerInfo);

//     return res.data.user;
//   } catch (error) {
//     return rejectWithValue(error.response.data);
//   }
// });
